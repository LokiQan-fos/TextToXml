---
epic: 3
date: 2026-09-11
verdict: accepted-with-open-items
criteria: declared
headless: false
---

# Rétrospective — Épic 3 : `Kape22Importer` — worker, orchestration & exploitation

> Scrum Master / Lead Architecte .NET — méthode BMAD
> Rétrospective conduite sur preuves : `epics.md` (Épic 3 + ledger « AC reportés d'Épic 2 »),
> `epic-3-context.md`, `sprint-change-proposal-2026-09-09.md`, `deferred-work.md`,
> `sprint-status.yaml`, historique git (18 commits `7629c5b^..0c27742`, 0 merge) **et**
> historique SVN du dépôt voisin `MicroServices.sln` (r528–r533), suites xUnit `Unit`
> **et** `Integration` ré-exécutées dans les deux dépôts.

---

## 1. Résumé de l'épic

| Élément | Valeur |
|---|---|
| Objectif | Un worker supervisé par le `Launcher` du portail scrute le dossier de réception, orchestre chaque Fichier (lecture → `TextToXml` → archive XML → `Kape22Mapper` → EF → déplacement), journalise deux fois (`MQTTnetServices.Logs` + `L_D_LOG_COMMANDE`), survit aux pannes de boucle, et est validé de bout en bout |
| Stories | **7 / 7 `done`** (3.0 → 3.6) — `pending_stories` vide (`detect-epic --epic 3`) |
| Plage de diff (ce dépôt) | `7629c5b^..0c27742` — 18 commits, **0 merge**. Périmètre retenu : premier commit de story 3.1 (`7629c5b`) → dernier commit de story 3.6 (`0c27742`) |
| Hors périmètre du diff | `e267386` / `96ed233` (2026‑09‑07, parité legacy FR‑7) exclus : datés avant le premier commit de story 3.1, rattachés à `spec-parity-kape22-legacy-fill-rules.md`, périmètre FR‑7 (Épic 2) |
| Correction de cap | **2026‑09‑09** (`sprint-change-proposal-2026-09-09.md`) : Story 3.4 initiale rejetée en revue (infra Launcher dupliquée) ; `Kape22Importer` redevient une **bibliothèque**, le worker est une classe mince `Client : Publisher` enregistrée dans le `Launcher` (`MicroServices.sln`). Story 3.0 ajoutée ; 3.1/3.2 réutilisées telles quelles ; 3.3 revisitée (seam de journalisation) ; 3.4/3.5/3.6 réécrites |
| FRs couverts | FR‑12, FR‑13, FR‑14, FR‑15, SM‑1/2/3, NFR‑1/2 (+ 4 `AC` reportés d'Épic 2 réconciliés en tête d'épic dans `epics.md`) |
| Statut sprint | `epic-3: in-progress` dans `sprint-status.yaml` — **incohérent** : les 7 stories sont `done`, à basculer `done` (cf. §8) |
| Second dépôt impliqué | `MicroServices.sln` (SVN, `C:\Users\Administrateur\Documents\MicroServices`) : `GPAO/ImportP60` (`Client`) + `Launcher/WorkerRegistry.cs` + `Launcher.Tests`. Révisions **r528–r533** (2026‑09‑09 → 2026‑09‑11) |
| Artefacts de story | Quasi absents comme à l'Épic 2 — pas de fichiers `stories/3-x.md` ; deux specs de référence (`spec-3-0-*.md`, `spec-3-4-*.md`). La spec de référence reste `epics.md` + `epic-3-context.md` |
| Logs de session | Non disponibles → la « cérémonie rouge » de CC-1 n'est vérifiable que par le corps des commits (voir §3) |

### Inventaire des preuves

| Disponible | Manquant / non vérifié |
|---|---|
| `epics.md` (Épic 3 complet + ledger AC reportés + CC-1…CC-7) | Fichiers de story individuels (`stories/3-x.md`) |
| `epic-3-context.md`, `sprint-change-proposal-2026-09-09.md`, `spec-3-0-*.md`, `spec-3-4-*.md` | Logs de session / transcripts (aucun disponible pour l'épic) |
| `deferred-work.md` — ledger daté de toutes les revues (story-0 hygiène, 3.0, 3.1, 3.2, 3.5, 3.6) | — |
| `git_evidence.py` sur `7629c5b^..0c27742` : 18 commits, 0 merge, churn par fichier | — |
| Suite xUnit `Category=Unit` (ce dépôt) : **500 verts** (192 `TextToXml.Tests` + 308 `Kape22Importer.Tests`), ré-exécutée | — |
| Suite xUnit `Category=Integration` (ce dépôt) : **127 verts / 1 échec** sur ce poste, ré-exécutée — échec **environnemental**, voir §5 | Aucun gate CI sur cette catégorie (F‑1, régression Épic 2 non résolue) |
| `MicroServices.sln` (SVN) : `GpaoImportP60.Tests` **10/10** verts, `Launcher.Tests` **2/2** verts, ré-exécutés indépendamment | — |
| Historique SVN `svn log -v` r528…r533 (messages de commit, fichiers touchés) | — |
| Rétro Épic 2 + `action_items` de `sprint-status.yaml` (12 items : 9 `done`, 3 `open`) | — |
| Code source `src/Kape22Importer/` (17 fichiers, 1 965 LOC) relu | — |

---

## 2. Bilan quantitatif

### 2.1 Stories

7 stories livrées (3.0 → 3.6), une par commit squashé sauf 3.2 (3 commits : livraison, passe revue, patches) et 3.3 (2 commits). Entre 3.2 et 3.3, 4 commits `chore(epic-3)` portent la passe d'hygiène « story 0 » héritée de l'action A‑1 de la rétro Épic 2 (split `Kape22Mapper`, réconciliation des `AC` reportés). Chaque commit de story touche `deferred-work.md` + `sprint-status.yaml`, convention tenue sur tout l'épic.

### 2.2 Évolution des tests xUnit (rouge → vert)

Décompte des attributs `[Fact]/[Theory]/[SkippableFact]` à chaque commit de l'épic (ce dépôt) :

| Commit | Story | `Kape22Importer.Tests` | `TextToXml.Tests` | Événement |
|---|---|---:|---:|---|
| `c0cb72e` | (fin Épic 2) | 104 | 139 | référence de départ |
| `7629c5b` | 3.1 | 134 | 139 | `InboxScannerTests`, `DirectoryFileSourceTests`, `ImportOptionsTests` |
| `259b3d2` | 3.2 | 141 | 139 | `Kape22FichierProcessorTests` |
| `c43e297` | 3.2 (revue) | 148 | 139 | + `Kape22FichierProcessorIntegrationTests` (provider InMemory, D1) |
| `dd98349` | story‑0 (A‑1‑b) | 148 | 139 | split `Kape22Mapper` — refactor pur, 0 test net |
| `14a42cc` / `841d299` | 3.3 | 163 | 139 | `DoubleJournalTests`, `DoubleJournalIntegrationTests` |
| `eeb84f9` | 3.0 | 158 | 140 | suppression host (`StartupWiringTests`, `AscoLsiConnectionConfigTests` retirés) ; `SolutionStructureTests` renforcé |
| `8eb735c` | 3.4 | 159 | 140 | `Tick_WhenTheTokenIsCancelledAfterAFichier_..._AcFr14_6` |
| `33b22cb` / `f20c36e` | 3.0 / 3.4 (revue) | 159 | 140 | corrections, 0 test net |
| `8b5d90d` | 3.5 | 176 | 140 | `WorkerLoopRobustnessTests`, `WorkerLoopRobustnessIntegrationTests` |
| `0c27742` | 3.6 | 183 | 140 | `AcCoverageCompletenessTests`, `EndToEndImportIntegrationTests`, `EndToEndPerformanceTests`, `ErrorsReportReadabilityTests` |

**État final (ce dépôt, ré-exécuté)** : `dotnet test --filter Category=Unit` → **500 verts, 0 échec** (192 `TextToXml.Tests` + 308 `Kape22Importer.Tests`, ~2,4 s). `Kape22Importer.Tests` passe de **104 → 308** cas verts sur l'épic (attributs 104 → 183, le grossissement supplémentaire venant des `[Theory]`/`InlineData`), croissance monotone, aucune régression réintroduite.

**État final (`MicroServices.sln`, SVN, ré-exécuté indépendamment)** : `GpaoImportP60.Tests` **10/10** verts, `Launcher.Tests` **2/2** verts — confirme les chiffres du commentaire `sprint-status.yaml` du 2026‑09‑11.

**Cérémonie rouge → vert** : signaux positifs explicites dans **11 des 13** corps de commit (`3.2` livraison, `3.2` revue avec décisions D1‑D3, `3.3` ×2, `3.4` ×2 revue, `3.5` revue, `3.6` revue — chacun liste `AC → test`). **Deux exceptions, comme à l'Épic 2 (F‑14)** : `7629c5b` (3.1, seul commit de la story) et `249b6e9` (« 3.2 : move to review ») ont un corps de commit réduit au `Co‑Authored‑By`. L'action rétro Épic 2 **A‑4** (garde sur le corps de commit) est restée `open` tout l'épic (voir §4, F‑2) et la même omission s'est reproduite.

### 2.3 Couverture des critères d'acceptation (`AC-FRx-y` / `SM-x`)

Deux gates actifs et vérifiés :

1. `AcTraitCoverageTests` (Épic 1) — casse le build si un test nommé d'après un `AC` n'a pas le `[Trait]` correspondant.
2. `AcCoverageCompletenessTests.EveryKape22ImporterAcHasAtLeastOneTrait_Sm1` (**nouveau, Story 3.6**) — sens inverse : casse le build si un `AC-FRx-y` du catalogue FR‑7..FR‑15 n'a **aucun** test porteur du trait, avec deux exclusions documentées (`FR9-6`, hors assembly ; `FR14-5`, hors solution).

| FR | Périmètre Épic 3 | Traits présents | Verdict |
|---|---|---|---|
| FR‑12 | AC-FR12-1..12-9 | 9/9 (story 3.1) | ✅ complet |
| FR‑13 | AC-FR13-1..13-5 | 5/5 (story 3.2) | ✅ complet |
| FR‑14 | AC-FR14-1..14-8 | 7/8 dans ce dépôt ; **`AC-FR14-5` couvert dans `MicroServices.sln`** (`Launcher.Tests.WorkerRegistryTests.WorkerRegistry_RegistersGpaoImportP60AsAWorkerAdapter_AcFr14_5`, vérifié ré-exécuté) | ✅ complet (cross-repo) |
| FR‑15 | AC-FR15-1..15-4 | 4/4 (story 3.5) | ✅ complet |
| SM‑1/2/3 | agrégateur, E2E 10 fichiers, `*.errors.json` lisible | 3/3 (story 3.6) | ✅ complet |
| NFR‑1/2 | < 200 ms/fichier, < 30 s/500 fichiers | tests présents (`EndToEndPerformanceTests`), non tracés par `[Trait("AC", …)]` — SM‑1 ne l'exige pas (portée FR‑x/CTR‑x uniquement) | ✅ conforme au texte SM‑1 |

**Réconciliation des 4 `AC` reportés d'Épic 2** (ledger `epics.md`, action A‑3/item‑10 de la rétro Épic 2) : les quatre sont maintenant **attachés et vérifiés** — `AC-FR6-4` étendu à `ImportResult` (listes `Errors`/`Warnings` indépendantes, chacune triée par `LineNumber`, story 3.3) ; volet journalisation de `AC-FR10-7`/`AC-FR11` = `AC-FR14-1`/`AC-FR14-8` (story 3.3) ; `AC-FR12-3` fait (story 3.2, résiduel `InboxScanner` non consommateur — accepté, seam story 3.3) ; skip‑doublon explicite = `ImportResult.AlreadyImported` (story 3.3). **Aucun `AC` nommé ne franchit la frontière Épic 3 → Épic 4 sans requalification** — contrairement à l'Épic 2 → Épic 3, aucune nouvelle dette de ce type n'a été identifiée.

**Réserve documentaire** (F‑5, mineure) : le commentaire d'exclusion `FR14-5` dans `AcCoverageCompletenessTests.cs` (commit `0c27742`) affirme que le test manque encore dans `MicroServices.sln` alors que **le même commit** documente dans `deferred-work.md` l'avoir retrouvé dans `Launcher.Tests`. Voir §4.

### 2.4 Volumétrie du code (churn `7629c5b^..0c27742`, hors `bin/obj`)

| Fichier source | net | commits | Observation |
|---|---:|---:|---|
| `InboxScanner.cs` | +325 | 5 (3.1, 3.2, 3.5, 3.6, revue) | 325 LOC, plus gros fichier écrit à la main de l'assembly. Une seule responsabilité cohérente (cycle de vie du Fichier : scan → dispatch → archive/reject → purge) — pas d'accrétion hétérogène comme `Kape22Mapper` à l'Épic 2, mais à surveiller si l'Épic 4 y ajoute encore une couche (F‑4) |
| `Kape22FichierProcessor.cs` | +197 | 5 (3.2 ×3, 3.3, 3.6) | Orchestrateur par Fichier, cohésif (ordre strict lecture → convert → map → persist) |
| `CoherenceChecker.cs` | +132 | 1 (story‑0 A‑1‑b) | Nouveau, issu du split `Kape22Mapper` — FR‑10 |
| `DerivedFields.cs` | +97 | 1 (story‑0 A‑1‑b) | Nouveau, issu du split `Kape22Mapper` — FR‑9 |
| `Kape22Mapper.cs` | **‑178** | 3 (3.3, story‑0, revue) | 325 → 184 LOC. **Action A‑1(b) de la rétro Épic 2 confirmée livrée** : classe `sealed`, `Map` d'instance, `TimeProvider` au constructeur — l'API de référence PRD est désormais respectée (F‑2 de la rétro Épic 2 clos) |
| `DirectoryFileSource.cs` | +80 | 1 (3.1) | Implémentation `IFileSource` de production, déférée de l'Épic 2 (F‑11 / notes story 3.1) |
| `ImportOptions.cs` | +37 | 1 (3.1) | Config FR‑12, CC‑4 vérifié |
| `IFileSource.cs` | +42 | 1 (3.1) | Seam, in‑memory en test |
| `TestSupport.cs` (test) | +134 | 4 | Extension continue du helper partagé Épic 2 (action A‑1‑a) |
| — suppressions (Story 3.0, conversion en bibliothèque) — | | | |
| `Program.cs`, `Worker.cs`, `StartupCompatibilityHostedService.cs`, `StartupServiceCollectionExtensions.cs`, `PersistenceServiceCollectionExtensions.cs`, `appsettings.Development.json`, `launchSettings.json` | ‑122 cumulé | 1 (3.0) | Host `Microsoft.NET.Sdk.Worker` entièrement retiré ; `Kape22Importer.csproj` passe en bibliothèque pure |
| `AscoLsiConnectionConfigTests.cs`, `StartupWiringTests.cs`, `PersistenceTestSupport.cs` | ‑194 cumulé | 1 (3.0) | Tests de câblage host supprimés avec le host (pas recréés, conforme AC Story 3.0) |

Aucun fichier ne dérive en god-class incontrôlée. `InboxScanner` (325 LOC) est le point à surveiller pour l'Épic 4 (F‑4), par analogie avec `Kape22Mapper` à l'Épic 2 — mais sa taille vient d'une seule responsabilité assumée (cycle de vie), pas d'un empilement de préoccupations orthogonales.

---

## 3. Audit des standards transverses (CC-1 → CC-7)

| Règle | Verdict | Preuve |
|---|---|---|
| **CC-1 — TDD strict** | 🟡 **Résultat : conforme / Cérémonie : même régression qu'à l'Épic 2** | Volet résultat : gate `AcTraitCoverageTests` + nouveau gate inverse `AcCoverageCompletenessTests` (SM‑1, « sans lacune »), 500 tests verts, chaque `AC` du périmètre nommé et testé. Volet cérémonie : corps de commit détaillé (`AC → test`, rouge avant code) sur **11/13** commits de code ; **`7629c5b` (3.1) et `249b6e9` (« 3.2 : move to review »)** ont un corps vide — **même motif que F‑14 de la rétro Épic 2**, dont l'action corrective (A‑4, garde pre-commit) est restée `open` tout l'épic (F‑2 ci-dessous) |
| **CC-2 — Commentaires anglais, phrase capitalisée + point, pas de liste numérotée** | 🟡 **Conforme, même réserve mineure qu'à l'Épic 2, non corrigée** | Scan `src/Kape22Importer/*.cs` : aucun commentaire de logique en français. **`CoherenceChecker.cs:18,72`** réutilise `Entête`/`Pied` (hors des 6 noms de domaine sanctionnés — `Champ`, `Ligne`, `Bloc`, `Fichier`, `Descripteur`, `Segment`, cf. mémoire `comment-language-cc2`) — copié tel quel de l'ancien `Kape22Mapper.cs` lors du split (A‑1‑b), la réserve « cosmétique » de la rétro Épic 2 n'a pas été purgée à cette occasion. `REJETÉ` cité comme valeur littérale (acceptable, cf. Épic 2) |
| **CC-3 — Commentaires sur leur propre ligne, au-dessus du bloc, pas de trailing** | 🟢 Conforme | `grep` trailing-comment sur les 17 fichiers `src/Kape22Importer/` neufs/modifiés : aucun |
| **CC-4 — Tri alphabétique des propriétés** | 🟢 **Conforme** | Vérifié manuellement sur les 5 nouveaux types de contrat : `ImportOptions` (ArchiveFolder…RetentionDays), `FichierProcessingResult` (Errors, NormalizedXml, Success, Warnings), `ImportResult` (AlreadyImported…XmlArchivePath) — tri case‑insensitive conforme, cohérent avec la mémoire `cc4-alphabetical-semantics`. Aucune dérogation R‑7‑like observée cet épic |
| **CC-5 — Vocabulaire du glossaire à l'identique** | 🟢 Conforme | `Fichier`, `Bloc`, `Descripteur`, `ImportResult`/`MapResult`/`ConversionResult` (même forme dérivée `Success`), noms de test citant leur `AC-FRx-y`/`SM-x` |
| **CC-6 — Pureté `TextToXml`** | 🟢 **Conforme, renforcée** | `src/TextToXml/Contract.cs` gagne `ErrorCode.SchemaInvalid` et `ErrorCode.UnexpectedFailure` (story 3.5) — deux codes **génériques** (échec de schéma, échec imprévu), pas de vocabulaire P60. `SolutionStructureTests` renforcé en story 3.0. `TextToXml.csproj` toujours zéro `PackageReference` propre au métier P60 |
| **CC-7 — Secrets** | 🟢 Conforme | `ConnectionStrings:AscoLSI`/`MQTTnetServices` vides dans `appsettings.json`/`GpaoImportP60.json` (renseignées au déploiement) ; `Import:InboxPath` est un chemin de config, pas un secret ; aucune chaîne de connexion en dur trouvée dans le code relu |

**Synthèse** : la rigueur transverse **se maintient** au niveau de l'Épic 2 — CC‑3 à CC‑7 sans écart. Les deux entorses sont des **récidives non corrigées** de réserves déjà connues : (a) corps de commit vide sur 2 commits sur 13 (CC‑1, même pattern que F‑14, action A‑4 toujours `open`), (b) `Entête`/`Pied` en commentaire (CC‑2, copié depuis l'ancien code lors du split au lieu d'être purgé). Aucune dérive nouvelle.

---

## 4. Constats (findings) avec source

| # | Constat | Source | Disposition |
|---|---|---|---|
| **F‑1** | **La catégorie `Integration` n'a toujours aucun gate CI — deuxième épic consécutif.** `ci.yml` tourne sur `ubuntu-latest` avec `--filter Category=Unit` seul (commentaire explicite : « le runner … est toujours une décision d'infra ouverte »). L'action A‑2/item‑9 de la rétro Épic 2 est restée `open` tout l'Épic 3, alors que ce dernier ajoute justement la logique la plus critique pour la mise en production (double journalisation FR‑14, robustesse FR‑15, E2E SM‑2) — tout entière non gatée. | `.github/workflows/ci.yml` ; `sprint-status.yaml` (item `epic-2-retro-item-9`, `status: open`) | **Fix now → Épic 4** (reconduit tel quel depuis la rétro Épic 2) |
| **F‑2** | **Un échec `Category=Integration` réel, reproduit en direct sur ce poste, illustre exactement le risque documenté et jamais traité depuis la revue de story 2.1** : `PersistenceSmokeTests.SchemaApplies_CreatesExactlyTheHarnessTables` échoue (`UserTableCount(MQTTnetServices) == 2`, attendu `1`) — le script `scripts/schema/02-mqtt-tables.sql` ne crée plus que `dbo.Logs` depuis la correction de cap (`dbo.WorkerSettings` retiré, Launcher‑owned), mais ses gardes `IF OBJECT_ID(...) IS NULL` ne suppriment jamais une table héritée d'un état antérieur de la base de test locale. C'est exactement le scénario « le schéma de test n'est jamais réinitialisé entre deux runs » signalé dès la revue de story 2.1 et jamais corrigé. Aucune régression de code — un problème d'environnement local non réinitialisé, mais qui prouve que le risque est réel et non plus théorique. | Exécution `dotnet test --filter Category=Integration` (ce jour) ; `scripts/schema/02-mqtt-tables.sql:11-13` ; `deferred-work.md` (« Deferred from: code review of story 2.1 ») | **Fix now → Épic 4** : ajouter un drop/recreate des tables du harnais dans `SqlServerIntegrationFixture` (même correctif que F‑1/A‑2 de la rétro Épic 2, jamais fait) |
| **F‑3** | **La garde sur le corps de commit (action A‑4 de la rétro Épic 2, `open`) n'a jamais été construite, et la même omission s'est reproduite** : `7629c5b` (3.1, seul commit de la story) et `249b6e9` (« 3.2 : move to review ») ont un corps de commit réduit au `Co‑Authored‑By`, exactement le motif F‑14 de l'Épic 2. Les 3 coquilles `Description` de `Templates/P60.xml` (`Rmetteur`, « tolérence » ×2), autre volet de la même action A‑4, sont également **toujours en place**. | `git log 7629c5b -1 --format=%b` ; `git log 249b6e9 -1 --format=%b` ; `Templates/P60.xml:15,35,36` ; `sprint-status.yaml` (item `epic-2-retro-item-11`, `status: open`) | **Fix now → Épic 4** (reconduit tel quel, deuxième occurrence) |
| **F‑4** | **`InboxScanner` concentre désormais la plus grosse surface écrite à la main de l'assembly (325 LOC, 5 commits sur l'épic).** Une seule responsabilité assumée (cycle de vie du Fichier), pas un empilement hétérogène comme `Kape22Mapper` avant l'Épic 2 — mais c'est le fichier qui a le plus grossi sur l'épic et le seul touché par 3 stories différentes (3.1, 3.5, 3.6) en plus de sa revue. À surveiller si l'Épic 4 y ajoute encore une couche. | `git_evidence.py` (`InboxScanner.cs` net +325, 5 commits) ; relecture `src/Kape22Importer/InboxScanner.cs` | **Accept** — point de vigilance pour la prochaine rétro, pas une action |
| **F‑5** | **Commentaire d'exclusion périmé dans le nouveau gate `AcCoverageCompletenessTests` — incohérence interne au même commit.** `AcCoverageCompletenessTests.cs:40‑43` (commit `0c27742`) affirme que le test `AC-FR14-5` « n'existe pas encore » dans `MicroServices.sln`, alors que **le même commit** documente dans `deferred-work.md` l'avoir retrouvé dans `Launcher.Tests/WorkerRegistryTests.cs` (`WorkerRegistry_RegistersGpaoImportP60AsAWorkerAdapter_AcFr14_5`, ré-exécuté ici, vert). L'exclusion `KnownExceptions` reste architecturalement correcte (l'assembly `Kape22Importer.Tests` ne peut pas voir un trait porté par une autre solution), mais son commentaire justificatif est faux. | `tests/Kape22Importer.Tests/AcCoverageCompletenessTests.cs:40-43` vs `deferred-work.md` (« Deferred from: code review of story-3.6 », note résolue le même jour) ; ré-exécution `Launcher.Tests` (2/2 verts) | **Fix now** (trivial — corriger le commentaire, garder l'exclusion) |
| **F‑6** | **L'audit trail SVN (`MicroServices.sln`) ne porte l'attestation `AC → test` que depuis la fin de l'épic.** Les révisions `r528` à `r532` (2026‑09‑09/10 : création `GPAO/ImportP60`, câblage `Launcher/WorkerRegistry.cs`, création `Launcher.Tests`) ont un message de commit **vide**. Seule `r533` (2026‑09‑11, la revue de story 3.6) porte un message complet listant les `AC` couverts, symétrique à la convention du dépôt git. La discipline s'est donc installée **en fin d'épic seulement**, côté SVN. | `svn log -v` sur `MicroServices.sln`, r528‑r533 | **Fix now → Épic 4** : appliquer dès le premier commit SVN de l'Épic 4 la même convention `AC → test` que côté git |
| **F‑7** | **Aucune borne poison-pill dans `InboxScanner`, aucun garde individuel sur `PurgeRetention.Delete`, sonde de stabilité sans délai inter-probe** — trois écarts déjà identifiés en revue de story 3.5 (2026‑09‑10), non corrigés (acceptés comme non bloquants par la revue elle‑même, `AC-FR15-x` ne les exige pas). | `deferred-work.md` (« Deferred from: code review of story-3.5 ») | **Defer** — repris ici pour continuité de traçabilité, aucune action nouvelle |
| **F‑8** | **Câblage `Client` sans conteneur DI : plusieurs chemins d'erreur non alignés sur l'intention** — une faute non‑`StartupCompatibilityException` dans `CreateAsync` échappe au `catch` dédié ; `Client.Dispose()` n'annule pas le `CancellationTokenSource` hors du chemin `Stop()` ; `WorkerAdapter.IsRunning` reste vrai après un échec FR‑8 ou un broker jamais connecté, sans `LastError` renseigné (tableau de bord trompeur). | `deferred-work.md` (« Deferred from: code review of spec-3-4-gpao-importp60-client-worker ») | **Defer → Épic 4**, si le tableau de bord Launcher devient un point d'exploitation actif |

**Vues d'agrégat — architecture** : la conversion `Kape22Importer` en bibliothèque pure (Story 3.0) est le changement structurel majeur de l'épic — cohérente avec `AC-FR16-1` (toujours vert), zéro dépendance host résiduelle vérifiée par `SolutionStructureTests`. Le split `Kape22Mapper` → `CoherenceChecker`/`DerivedFields` (action A‑1‑b, Épic 2) est confirmé livré et a effectivement réduit la classe de 325 à 184 LOC.

**Revue de diff (lentilles code)** : `bmad-review` n'a pas été relancé sur le diff d'épic complet — chaque story a déjà subi sa passe de revue adverse à 4 couches (consolidée dans `deferred-work.md`), et l'Épic 3 a en plus une re‑revue explicite de story 3.5 et 3.6 (toutes deux initialement **refusées** puis corrigées, cf. corps de commit `8b5d90d`/`0c27742`). Périmètre restreint assumé : relecture inline des 17 fichiers `src/Kape22Importer/` + exécution des deux suites (`Unit` et `Integration`, ce dépôt) + des deux suites `MicroServices.sln` (SVN) au lieu d'une nouvelle passe multi-agent. Aucun constat *correctness* nouveau au‑delà de F‑1…F‑8 n'a émergé de cette relecture. Les frontières entre stories sont couvertes par F‑4 (accrétion `InboxScanner`) et F‑8 (câblage `Client` sans hôte, à la couture 3.0↔3.4).

---

## 5. Vérification du comportement

L'Épic 3 change le comportement runtime dans **deux dépôts**. Flux exercés de bout en bout aujourd'hui :

- `dotnet test TextToXml.sln --filter Category=Unit` → **500 passés, 0 échec, 0 ignoré** (~2,4 s).
- `dotnet test TextToXml.sln --filter Category=Integration` → **127 passés, 1 échec** contre l'instance SQL Server locale :
  - **Échec** : `PersistenceSmokeTests.SchemaApplies_CreatesExactlyTheHarnessTables` — la base de test `MQTTnetServices_Test` locale porte une table héritée en trop (voir F‑2). Confirmé **environnemental**, pas une régression de code : tous les autres tests `DoubleJournalIntegrationTests`, `EndToEndImportIntegrationTests`, `WorkerLoopRobustnessIntegrationTests`, `TransactionalPersistenceTests` passent contre la même instance.
  - `EndToEndImportIntegrationTests` (SM‑2) : les 10 fichiers `P60/P60_847_682_001..010` insèrent bien 10 lignes `L_D_KAPE22` + 10 lignes `L_D_LOG_COMMANDE` ` — OK` cohérentes.
  - `WorkerLoopRobustnessIntegrationTests` (AC‑FR15‑3/15‑4) : base injoignable → Fichiers laissés en `processing/`, reprise sans doublon vérifiée contre le garde‑fou D22 réel.
- `dotnet build TextToXml.sln -warnaserror` (configuration Release) → **0 warning, 0 erreur** — le gate mécanique CC‑2/CC‑3/CC‑4 reste vert.
- `MicroServices.sln` (SVN), ré-exécuté indépendamment de ce dépôt :
  - `dotnet test GPAO/ImportP60.Tests` → **10/10 passés** — `Client.RunTickCore` exercé avec le vrai `Kape22FichierProcessor` sur EF InMemory et le fixture réel `P60_847_682_001`.
  - `dotnet test Launcher.Tests` → **2/2 passés**, dont `WorkerRegistry_RegistersGpaoImportP60AsAWorkerAdapter_AcFr14_5`.
- **Non exercé** : `Client.CreateAsync()` lui‑même (ouvre un vrai sink Serilog SQL, un vrai broker MQTT et une vraie connexion SQL Server pour la porte FR‑8) — noté explicitement non testable en l'état par la revue de story 3.6 elle‑même ; c'est un choix de portée assumé, pas un oubli.

---

## 6. Suivi de la rétrospective précédente (Épic 2)

12 `action_items` de la rétro Épic 2 dans `sprint-status.yaml` : **9 `done`, 3 `open`**. Vérification par preuve :

| Item | Statut fichier | Preuve | Statut argumenté |
|---|---|---|---|
| `epic-2-retro-item-8` (hygiène story‑0 : `TestSupport`, split `Kape22Mapper`) | done | `Kape22Mapper.cs` 184 LOC (`sealed class`, `Map` d'instance) ; `CoherenceChecker.cs` / `DerivedFields.cs` présents ; commits `1c7e522`/`dd98349` | ✅ `done` confirmé |
| `epic-2-retro-item-9` (gate CI `Integration`) | **open** | `.github/workflows/ci.yml` inchangé, `Category=Unit` seul | 🔴 **toujours ouvert** — reconduit F‑1 |
| `epic-2-retro-item-10` (réconciliation `AC` reportés dans `epics.md`) | done | Ledger « AC reportés d'Épic 2 » en tête d'Épic 3 dans `epics.md` (lignes 1102‑1123), les 4 `AC` attachés et vérifiés (§2.3) | ✅ `done` confirmé |
| `epic-2-retro-item-11` (garde corps de commit + coquilles `P60.xml`) | **open** | Aucun hook `.git/hooks/`, `Templates/P60.xml` toujours fautif, 2 commits vides sur l'épic (`7629c5b`, `249b6e9`) | 🔴 **toujours ouvert** — reconduit F‑3, récidive observée |
| `epic-2-retro-item-12` (basculer `epic-2: done`) | done | `sprint-status.yaml` : `epic-2: done` | ✅ `done` confirmé |
| `epic-3-correct-course-2026-09-09-launcher-alignment` | **open** dans `sprint-status.yaml` | Toutes les actions listées sont **vérifiées exécutées** : Story 3.0 livrée (lib), 3.3 revisitée (seam journalisation via `Client`), 3.4/3.5/3.6 réécrites contre `WorkerAdapter`/`Client`, `scripts/schema/02-mqtt-tables.sql` sans `WorkerSettings`, `MicroServices.sln` bump `net10.0` fait (`dotnet --version` 10.0.400, build vert) | 🟡 **`open` obsolète** — proposé `done` (voir §8, confirmation utilisateur requise) |

**À proposer** : transition `epic-2-retro-item-8`, `-10`, `-12` déjà `done` (rien à faire) ; `epic-3-correct-course-2026-09-09-launcher-alignment` `open` → `done` (preuve ci‑dessus) ; `epic-2-retro-item-9` et `-11` restent `open`, reconduits tels quels comme actions Épic 4 (F‑1, F‑3).

---

## 7. Format Start / Stop / Continue

### 🟢 CONTINUE — ce qui a parfaitement fonctionné

1. **Le mécanisme de correction de cap a fonctionné exactement comme prévu.** La Story 3.4 initiale (infra Launcher dupliquée) a été **rejetée en revue avant d'être committée** — rien n'a été perdu, l'arbre de travail a été annulé proprement, `bmad-correct-course` a produit un plan daté et tracé (`sprint-change-proposal-2026-09-09.md`), et la ré‑implémentation contre le vrai modèle `Launcher` a livré 4 stories (3.0, 3.4, 3.5, 3.6) sans dette supplémentaire. C'est le processus de gouvernance de risque fonctionnant à son meilleur.
2. **Le gate SM‑1 « sans lacune » (`AcCoverageCompletenessTests`) referme une vraie faille de l'agrégateur existant** : `AcTraitCoverageTests` (Épic 1) ne détectait qu'un test mal étiqueté, jamais un `AC` du PRD totalement absent. Le nouveau test inverse cette logique et a immédiatement forcé une revue honnête de `AC-FR14-5` (F‑5) plutôt que de laisser un trou silencieux.
3. **Deux re‑revues adverses ont refusé le premier jet et corrigé avant merge** (story 3.5, story 3.6) — la discipline "refusée puis tous les correctifs appliqués" tient sur l'épic entier, pas seulement en fin de parcours.
4. **L'action A‑1(b) de la rétro Épic 2 (split `Kape22Mapper`) a été honorée intégralement**, pas seulement notée `done` : 325 → 184 LOC, API de référence PRD atteinte (`sealed class` + `Map` d'instance), zéro régression (592 tests inchangés au moment du split).
5. **La vérification croisée entre les deux dépôts a été faite en conscience** : le smoke test `Client` a délibérément renoncé à tester `CreateAsync()` (broker/SQL réels) et documenté pourquoi, plutôt que de fabriquer un test fragile ou trompeur.
6. **Pureté `TextToXml` tenue** : les deux seuls ajouts à la lib partagée (`SchemaInvalid`, `UnexpectedFailure`) sont des concepts génériques, pas du vocabulaire P60 — le test d'architecture `AC-FR9-6` continue de vivre dans `TextToXml.Tests`.

### 🔴 STOP — frictions et écarts

1. **Deux actions de la rétro Épic 2 sont restées `open` sur un épic entier et une s'est reproduite à l'identique.** Le gate CI `Integration` (item‑9) n'a pas avancé d'un pas alors que l'Épic 3 ajoute exactement la logique la plus critique (double journal, robustesse, E2E) sans filet automatisé — et le risque théorique documenté depuis la revue 2.1 (« le schéma de test n'est jamais réinitialisé ») **s'est concrétisé en direct** pendant cette rétrospective (F‑2). La garde sur le corps de commit (item‑11) n'a jamais été construite, et l'omission qu'elle devait prévenir (F‑14 à l'Épic 2) **s'est reproduite deux fois** (F‑3).
2. **L'audit trail SVN a démarré en retard.** Trois révisions structurantes (`r528`‑`r532`, la moitié de l'épic côté worker/Launcher) n'ont aucun message de commit ; seule la dernière (`r533`) porte l'attestation `AC → test` désormais standard côté git.
3. **Un commentaire de code s'est déjà périmé le jour même où il a été écrit** (F‑5) — la mise à jour de `deferred-work.md` et celle du commentaire source auraient dû être le même geste.
4. **Le statut `sprint-status.yaml` de l'action de correction de cap est trompeur** : marquée `open` alors que tout son contenu est vérifiablement livré — un lecteur du tableau de bord la croirait encore à faire.

### 🟡 START — à démarrer / automatiser pour l'Épic 4

1. **Construire réellement le gate CI `Integration`** — c'est la troisième fois que cette action est écrite (Épic 1 review 2.1, rétro Épic 2 A‑2/item‑9, ici F‑1). Runner Windows self‑hosted avec SQL Server Developer, **ou** service container Linux, **ou** a minima porter la logique de branchement (hors frontières de transaction réelles) sur SQLite/InMemory en `Category=Unit` — n'importe laquelle de ces trois options ferme un risque qui vient de se matérialiser (F‑2).
2. **Ajouter le drop/recreate des tables du harnais dans `SqlServerIntegrationFixture`** au moment où le point 1 est traité — c'est le correctif direct de F‑2, indépendant du choix de runner.
3. **Écrire le hook (ou l'étape de build) qui refuse un `chore(story-*)` sans lignes `AC → test`** — deux épics de suite ont montré que la mémoire d'équipe seule ne suffit pas (F‑14 Épic 2, F‑3 ici). Mécaniser, pas re‑demander.
4. **Étendre la convention `AC → test` dans le corps de commit au dépôt SVN dès le premier commit de l'Épic 4**, pas seulement à la revue finale (F‑6).
5. **Un réflexe « corriger le commentaire en même temps que la note de dette »** quand une revue résout un item `deferred-work.md` qui a un commentaire miroir dans le code (F‑5) — un item de check-list de fin de revue plutôt qu'un nouvel outil.

---

## 8. Plan d'actions Épic 4

| ID | Action | Détail | Owner | Statut |
|---|---|---|---|---|
| **A-1** | **Gate CI de la catégorie `Integration`** *(reconduite depuis la rétro Épic 2, action A‑2/item‑9)* | Runner Windows self‑hosted + SQL Server Developer **ou** service container Linux **ou**, à défaut, porter le branchement garde‑fou + la logique hors-transaction sur SQLite/InMemory en `Category=Unit`. Inclut le drop/recreate des tables du harnais dans `SqlServerIntegrationFixture` (correctif direct de F‑2). | Lead Architecte | open |
| **A-2** | **Garde sur le corps de commit + coquilles `P60.xml`** *(reconduite depuis la rétro Épic 2, action A‑4/item‑11)* | Hook pre-commit (ou étape build) refusant un `chore(story-*)` sans lignes `AC → test` dans le corps — 2 récidives en 2 épics (F‑14 Épic 2, F‑3 ici). Corriger les 3 coquilles `Description` de `Templates/P60.xml`. | SM | open |
| **A-3** | **Corriger le commentaire périmé de `AcCoverageCompletenessTests.cs`** | Lignes 40‑43 : remplacer « le test n'existe pas encore » par une référence à `Launcher.Tests.WorkerRegistryTests.WorkerRegistry_RegistersGpaoImportP60AsAWorkerAdapter_AcFr14_5`, en gardant l'exclusion `KnownExceptions` (architecturalement toujours correcte). | Dev | open |
| **A-4** | **Étendre la convention `AC → test` en corps de commit à `MicroServices.sln` (SVN) dès le premier commit** | F‑6 : la discipline n'est apparue qu'à la revue finale (r533) côté SVN ; l'appliquer dès le début d'Épic 4 sur ce dépôt. | Dev | open |
| **A-5** | **Purger `Entête`/`Pied` de `CoherenceChecker.cs`** | Réserve CC‑2 mineure reconduite depuis l'Épic 2, non traitée lors du split de `Kape22Mapper`. Remplacer par `Header`/`Footer` (ou les 6 noms de domaine sanctionnés). | Dev | open |
| **A-6** | **Basculer `epic-3: done` dans `sprint-status.yaml`** | Les 7 stories 3.x sont `done` mais l'épic reste `in-progress`. | SM | open |

**Transition de statut proposée (confirmation utilisateur requise avant écriture)** : `epic-3-correct-course-2026-09-09-launcher-alignment` (`open` → `done`) — toutes les actions listées sont vérifiées livrées (§6).

---

## 9. Verdict d'acceptation

### **ACCEPTED-WITH-OPEN-ITEMS** (critères *déclarés*)

**Critères déclarés** : Épic 3 de `epics.md` + `epic-3-context.md` + `sprint-change-proposal-2026-09-09.md` + AC de chaque story (FR‑12…FR‑15, SM‑1/2/3, NFR‑1/2) + ledger des 4 `AC` reportés d'Épic 2 + CC‑1…CC‑7.

**Preuves à l'appui de l'acceptation :**
- 7/7 stories `done`, `pending_stories` vide → le verdict machine n'est **pas** forcé à *rejected*.
- 100 % des `AC-FRx-y` du périmètre Épic 3 ont un test xUnit nommé et vert, **dans les deux dépôts** (500 `Unit` + 127 `Integration` ce dépôt, 10 + 2 `MicroServices.sln`, tous ré-exécutés indépendamment de la session qui a produit le code).
- Le nouveau gate SM‑1 « sans lacune » referme une vraie faille de couverture (F‑5 en est la preuve : il a immédiatement forcé une clarification honnête plutôt que de laisser un trou silencieux).
- Les 4 `AC` reportés d'Épic 2 sont réconciliés, attachés et vérifiés — aucune nouvelle dette de ce type identifiée pour l'Épic 4.
- Comportement runtime réellement exercé de bout en bout : E2E 10 fichiers, robustesse base injoignable, arrêt coopératif — dans les deux dépôts.
- La correction de cap du 2026‑09‑09 (rejet de la Story 3.4 initiale) a été gérée par le processus prévu, sans perte, avec un plan tracé.
- CC‑3 à CC‑7 : conformes, y compris la pureté `TextToXml` renforcée.

**Éléments ouverts (tous non bloquants, tracés en §8) :**
- **F‑1 / F‑2** : gate CI `Integration` toujours absent — **deuxième épic consécutif** — et le risque théorique associé vient de se matérialiser en direct (échec `PersistenceSmokeTests` reproduit ce jour, cause environnementale confirmée, pas une régression de code) → A‑1.
- **F‑3** : garde sur le corps de commit toujours absente, récidive de l'omission qu'elle devait prévenir → A‑2.
- **F‑5** : commentaire de code périmé le jour même de son écriture → A‑3.
- **F‑6** : audit trail SVN démarré en retard sur l'épic → A‑4.
- **CC‑2 (mineure)** : `Entête`/`Pied` non purgés lors du split `Kape22Mapper` → A‑5.
- **F‑4, F‑7, F‑8** : points de vigilance et durcissements différés, non bloquants pour l'Épic 4.

**Aucun constat bloquant.** Les deux écarts les plus significatifs (F‑1/F‑2 et F‑3) ne sont pas des régressions de l'Épic 3 — ce sont des actions de la rétro Épic 2 qui n'ont simplement pas avancé, et dont le coût de l'inaction est maintenant visible (un test a échoué en direct). C'est un signal net pour l'Épic 4 : reconduire ces deux actions telles quelles ne suffira plus une troisième fois sans un changement de mécanisme (mécaniser plutôt que documenter).

---

## 10. Questions ouvertes

| # | Question | Ce qu'une réponse changerait |
|---|---|---|
| Q-1 | Gate CI `Integration` : runner Windows self‑hosted, service container Linux, ou volet SQLite `Unit` pour la logique de branchement ? *(question non résolue depuis la rétro Épic 2)* | Coût et forme de A‑1 ; ce qui reste réellement non couvert en CI pour l'Épic 4. |
| Q-2 | La garde sur le corps de commit (A‑2) doit-elle être un hook `.git/hooks/pre-commit` local (non versionné, donc à réinstaller par poste) ou une étape de CI qui échoue après coup (`git log` sur le dernier commit) ? | Fiabilité réelle de la mécanisation — un hook local n'empêche rien si l'outil qui committe (agent ou humain) ne l'a pas installé. |
| Q-3 | `InboxScanner` (325 LOC, F‑4) : split préventif en Épic 4 (par exemple séparer le cycle de vie du Fichier de la purge de rétention), ou attendre qu'une story y ajoute effectivement une responsabilité orthogonale ? | Portée d'une éventuelle story d'hygiène en tête d'Épic 4. |
| Q-4 | `epic-3` passe `done` maintenant (A‑6), et la transition de statut de l'item de correction de cap (§8) est‑elle confirmée ? | Cosmétique `sprint-status.yaml`, mais conditionne l'écriture de cette rétro en Phase 5. |

---

## 11. Décisions de portée (run interactif)

- **Épic sélectionné** : 3, déduit du contexte (fichier ouvert par l'utilisateur : `sprint-status.yaml`, demande explicite « Épic 3 »). `detect-epic --epic 3` → `story_count: 7`, `pending_stories: []`, `retro_status: optional`. Confirmé par l'évidence.
- **Plage de diff** : `7629c5b^..0c27742` retenue — premier commit de story 3.1 au dernier commit de story 3.6. Exclut délibérément `e267386`/`96ed233` (2026‑09‑07, parité legacy FR‑7), datés avant le début de l'épic et rattachés à un spec séparé de périmètre Épic 2.
- **Second dépôt (`MicroServices.sln`, SVN)** : inclus dans le périmètre de preuve car l'épic livre du code de production là‑bas (le `Client` et son enregistrement Launcher) ; les deux suites de tests y ont été ré‑exécutées indépendamment plutôt que de faire confiance aux chiffres auto‑rapportés dans `sprint-status.yaml` — les deux concordent.
- **Périmètre de revue restreint assumé** : `bmad-review` non relancé sur le diff d'épic complet (chaque story, plus deux re‑revues, a déjà eu sa passe adverse à 4 couches, documentée dans `deferred-work.md`) ; relecture source inline des 17 fichiers `src/Kape22Importer/` + exécution des 4 suites de tests (2 dépôts × 2 catégories) à la place.
- **Session logs** : non disponibles pour cet épic (comme pour l'Épic 2) ; l'analyse « leçons de process » s'appuie uniquement sur `deferred-work.md` et les corps de commit.
