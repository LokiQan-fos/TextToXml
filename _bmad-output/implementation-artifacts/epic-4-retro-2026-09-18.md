---
epic: 4
date: 2026-09-18
verdict: accepted-with-open-items
criteria: declared
headless: false
---

# Retrospective — Epic 4: `Kape22Importer` — Transactional dispatch to the downstream tables (second pass)

> Scrum Master / Lead .NET Architect — BMAD method
> Conducted on evidence: `epics.md` (Epic 4, stories 4.1 through 4.9), `epic-4-context.md`,
> `ARCHITECTURE-SPINE.md`, `deferred-work.md`, `sprint-status.yaml`, `sprint-change-proposal-2026-09-17.md`,
> git history (28 commits, `ce6a7fc7^..HEAD`, 0 merges), xUnit `Unit` and `Integration` suites re-run this
> session, the prior `epic-4-retro-2026-09-17.md`, and the per-story `/run-review` aggregated reports for
> Story 4.3-bis and Story 4.9.

**Why a second retrospective for the same epic number:** the first Epic 4 retro
(`epic-4-retro-2026-09-17.md`, verdict accepted-with-open-items) landed while `epic-4` in `sprint-status.yaml`
was still `in-progress` at 7/7 stories (4.1–4.7) and one known blocking-for-production defect open
(`story-4-6-decimal-scale-defect-story-4-3-bis`). Its 5 action items (A-1..A-5) plus that defect drove a
correct-course (`_bmad-output/planning-artifacts/sprint-change-proposal-2026-09-17.md`, commit `d559f85`)
that added three more stories in strict sequence — 4.2-bis, 4.3-bis, 4.9 — before `epic-4` could be
re-marked `done`. This retro covers the full `4.1 → 4.9` arc, not only the reopening tail, since that is
the first point at which the whole epic — original scope plus its own correction — is complete.

---

## 1. Epic summary

| Item | Value |
|---|---|
| Goal | Unchanged from the first retro: dispatch a parsed P60 file into the 10 downstream `L_D_*` tables in one transaction with `L_D_KAPE22` (AD-1), explicit mappers (AD-2), every failure journalized (AD-4). |
| Stories | **10/10 `done`** (4.1–4.7, 4.2-bis, 4.3-bis, 4.9) — `pending_stories: []` (`detect-epic --epic 4`, `story_count: 10`) |
| Diff range | `ce6a7fc75af7b89695c7c2eaf6d5654cce44c323^..HEAD` — parent of story 4.1's first commit to `HEAD` (`65dfad6`, `chore(story-4.9): apply review patches`). **28 commits, 0 merges** (`git_evidence.py`; the first retro read the same left endpoint but a 16-commit, since-superseded `HEAD`). |
| Commits in range not attributed to any story | 12 of 28: `1b1d2e9`/`b4cf94d`/`a7fb70d`/`39642f2` (bmad tooling: `/build-story`, `/retro-epic`, `/run-review` persistence, slash-command customization), `98c0c85` (archive the approved Sprint Change Proposal), `d559f85` (plan the three post-retro correction stories), `bb46d8b` (the first retro's own commit) — process/administrative commits interleaved with story work, not epic deliverables. |
| FRs covered | FR-17, FR-18, FR-19, FR-20, FR-21 (unchanged) |
| Sprint status | `epic-4: done`, `epic-4-retrospective: done` (already set by the first retro; this retro's own finalize step re-confirms both and appends its own action items) |
| Story artifacts | No individual `stories/4-x.md` files; `epics.md` §Epic 4 (stories 4.1 through 4.9, each with its own Given/When/Then AC block) is the reference spec. Read in full for 4.9, 4.2-bis, 4.3-bis this session; 4.1–4.7 read in full by the first retro (not re-read line-by-line here — their AC text has not changed). |
| Session logs | Not available (same gap as every prior epic retro). |

### Evidence inventory

| Available | Missing / unverified |
|---|---|
| `epics.md` §Epic 4 (full AC text, stories 4.1–4.9), `ARCHITECTURE-SPINE.md` (AD-1..AD-7), `sprint-change-proposal-2026-09-17.md` (the correct-course decision record) | Individual `stories/4-x.md` files (none exist, as in the first retro) |
| `deferred-work.md` — dated ledger through "code review of story-4.9 (2026-09-18)", "code review of story-4.3-bis (2026-09-18)", and the "Resolved by: story-4.3-bis implementation" section | Session logs / transcripts |
| `git_evidence.py` on `ce6a7fc7^..HEAD`: 28 commits, 0 merges, per-file churn across the full 4.1–4.9 range | — |
| `dotnet build TextToXml.sln -warnaserror` (this session): 0 warnings, 0 errors | — |
| `dotnet test --filter Category=Unit` (this session): **673 green** (192 `TextToXml.Tests` + 481 `Kape22Importer.Tests`), 0 failed, 0 skipped | — |
| `dotnet test --filter Category=Integration` (this session): **140 green, 0 skipped**, 0 failed — against the local SQL Server harness (AR-12) | No live CI run inspected this session (same gap noted by the first retro) |
| Per-story `/run-review` aggregated reports: `reviews/story-4-3-bis/aggregated-report.md`, `reviews/story-4-9/aggregated-report.md` (both `ACCEPTÉ`) | Full-epic multi-lens `bmad-review` re-run — deliberately not repeated; see Diff-scope review note below |
| Previous retrospective (`epic-4-retro-2026-09-17.md`) — read in full for follow-through, this being the directly preceding retro for this same epic | `epic-3-retro-2026-09-11.md` — read for context per the invocation's standard N-1 resolution rule, but its items were already fully reconciled by the first Epic 4 retro (§4 there) and nothing in this pass touches Epic 3 scope; not re-verified again here |
| Source read directly this session: `DecimalScale.cs`, `Kape22Mapper.cs:108-115`, `Kape22ImportBundle.cs`, `Kape22ImportBundleMapper.cs:113`, `Kape22Persister.cs` (full), `MappingAnnexSchema.cs`, `MappingAnnexCompletenessTests.cs`, `annexe-mapping-dispatch-epic4.md`, `Kape22FichierProcessorTests.cs:200-231`, `Kape22ProductionDataParityTests.cs`, `scripts/e2e-worker-import.ps1`, `GpaoImportP60WorkerEndToEndTests.cs` | — |

**Diff-scope review, deliberately narrowed:** `bmad-review` was not re-invoked as a fresh multi-agent pass over the full 4.1–4.9 diff. Story 4.3-bis and Story 4.9 each already had their own formal `/run-review` pass this week (`reviews/story-4-3-bis/`, `reviews/story-4-9/`, both `ACCEPTÉ`), and 4.1–4.7's cross-story seam was the first retro's own `bmad-review` pass (its §2.3). What this retro adds instead is targeted verification that each of those reviews' *deferred* items — and the first retro's own A-1..A-5 — actually landed in the code, which a review pass over a diff cannot check (a deferred item is by definition not in the diff that fixed it, or, as found below, sometimes not in any diff at all). This is a narrower, cheaper, and more relevant check for a correction-arc retro than re-deriving findings a review already produced twice.

---

## 2. Findings

### 2.1 Previous retro's A-1 landed in full, but `sprint-status.yaml` never recorded it — a bookkeeping gap, not a delivery gap

**Source:** `sprint-status.yaml` carries `epic-4-retro-item-1-annex-scale-precision-field` at `status: open`, referencing `-> story 4-2-bis-extension-annexe-mapping-scale-precision`. Direct verification this session: `_bmad-output/implementation-artifacts/annexe-mapping-dispatch-epic4.md:13,44,106,200,214,227` — every table header now carries a 4th `Scale` column, with `annexe-mapping-dispatch-epic4.md:13` documenting the rule ("Obligatoire (et non `-`) pour toute colonne `decimal` sourcée d'un champ KAPE22 `int`/`int?`"). `tests/Kape22Importer.Tests/MappingAnnexSchema.cs:14,34,44,94,104-118` parses the `Scale` cell into `MappingAnnexEntry.Scale` and fails loudly on a malformed cell; `MappingAnnexSchema.cs:127-181` (the `ScaleCompletenessAgainstModel` family) cross-checks every `decimal`-from-`int` column in the real `AscoLsiDbContext` EF model against the annex, failing when `Scale` is missing. `tests/Kape22Importer.Tests/MappingAnnexCompletenessTests.cs:123-198` exercises the family: `DecimalFromIntWithoutScale_FailsCompleteness_AcFr17_5`, `DecimalFromIntWithScaleAndDecimalSourcedColumn_PassCompleteness_AcFr17_5`, `Parse_ReadsScaleCell_ZeroDashAndMalformed_AcFr17_5`.

**Disposition:** the finding is a **stale sprint-status entry**, not an open piece of work — Story 4.2-bis's own AC (`epics.md:2037-2079`) is fully met by the code above, and Story 4.3-bis (`epics.md:2097-2131`) explicitly built on this annex as "la référence unique du facteur d'échelle par colonne." Proposed for reconciliation to `done` in Phase 5 (see §4), citing the source lines above as evidence.

### 2.2 Two of the first retro's own A-items landed via pre-checks/constants exactly as specified, including the deliberate non-widening of A-5's exception filter

**Source:** A-2 — `tests/Kape22Importer.Tests/Kape22FichierProcessorTests.cs:218-230` now asserts all 9 downstream `DbSet`s plus `ConsignesRows` (`Assert.Single(verify.OrdreFabricationRows)` … `Assert.Equal(5, verify.ConsignesRows.Count())`), extending the two Unit-tier success cases the first retro flagged. A-3 — `src/Kape22Importer/Kape22Mapper.cs:112-113` (`entity.OF = entity.OF.Trim(); entity.Coulee = entity.Coulee.Trim();`) inside the reflective per-Champ loop, exactly where the first retro's finding said it belonged. A-4 — `src/Kape22Importer/Kape22ImportBundle.cs:19` (`public const string ColdConsignePits = "1";`), now the single source both `Kape22ImportBundleMapper.cs:113` and `Kape22Persister.cs:90` reference — grepping the whole `src/Kape22Importer/` tree for `CodeConsignePits ==` / `!= "1"` finds no remaining inline literal. A-5 — `Kape22Persister.cs:119-129` adds a `GroupBy(row => (row.OF, row.CodeOperation, row.TypeConsigne, row.ConsigneGPAO))` pre-check that raises a `ConversionError` + REJETÉ `L_D_LOG_COMMANDE` row before `AddRange`, and the exception filter at all four `catch` sites in the file (`Kape22Persister.cs:102,136,172,199`) is still exactly `when (exception is DbUpdateException or DbException)` — confirming `epics.md:2187-2192`'s explicit "n'est pas élargi" constraint was honored, not silently dropped for convenience.

**Disposition:** accept as landed. No further action.

### 2.3 The decimal-scale defect is fixed and independently proven by the previously-skipped E2E worker test now passing

**Source:** `src/Kape22Importer/DecimalScale.cs` (`Apply(int rawValue, int scale) => rawValue / Pow10ForScale(scale)`), referenced from all 21 call sites across the 5 affected mappers (`OrdreFabricationMapper.cs:41-59`, `SectionChargeChutageMapper.cs:22-23`, `SectionChargeDecoupeMapper.cs:23`, `SectionChargeLingotMapper.cs:23-32`, `SectionChargePitsMapper.cs:23`) with literal scale arguments matching the annex's `Scale` column. `TestSupport.ZeroOutOfScaleDimensions`/`OutOfScaleDimensionFields` — the Story 4.6-era workaround that mutated fixtures to dodge the overflow — no longer exists anywhere in `tests/Kape22Importer.Tests/` (grep returns nothing). This session's `dotnet test --filter Category=Integration` ran **140/140 green, 0 skipped** — up from the first retro's **136/137, 1 skipped**; the one prior skip, `GpaoImportP60WorkerEndToEndTests`, now runs and passes against the real, unmutated `P60_847_682_081/082` fixtures (confirmed `[SkippableFact]` at `GpaoImportP60WorkerEndToEndTests.cs:38` still guards only on SQL Server availability, `Skip.IfNot(fixture.Available, ...)`, not on any scale-specific condition).

**Disposition:** accept as landed — closes `story-4-6-decimal-scale-defect-story-4-3-bis`, already `done` in `sprint-status.yaml`. This was the epic's single blocking-for-production item; it no longer blocks.

### 2.4 Four of Story 4.3-bis's own deferred review items point their `ref` at Story 4.9, but Story 4.9's actual declared scope never covered them — they are still genuinely open

**Source:** `sprint-status.yaml` carries `story-4-3-bis-review-item-1` (scale-literal triplication across mapper/test/annex), `-item-2` (no anti-bypass guard against a future 6th mapper skipping `DecimalScale.Apply`), `-item-3` (production parity only round-trips `L_D_KAPE22`), `-item-4` (`e2e-worker-import.ps1`'s closing `Kape22ProductionDataParityTests` check never inspects `$LASTEXITCODE`) — all four `status: open`, all four `ref` pointing at `-> story 4-9-hardening-epic-4`. But `epics.md:2135-2202` (Story 4.9's actual, frozen AC — read in full this session) scopes the story to exactly four items labeled A-2 through A-5, none of which is any of the above; they are the first retro's own §2.3 findings (downstream-table asserts, OF/Coulee trim, shared constant, persister pre-check), not the Story 4.3-bis review's four deferred items. Direct verification this session confirms none of the four landed: `grep -rn "DecimalScale" tests/Kape22Importer.Tests/*.cs` shows no test comparing a mapper call site's literal scale argument against the annex's `Scale` value (item-1) and no reflection/analyzer-based completeness guard against bypassing `DecimalScale.Apply` (item-2); `Kape22ProductionDataParityTests.cs:141` still reads only `typeof(L_D_KAPE22).GetProperties()` (item-3); `scripts/e2e-worker-import.ps1` has exactly one `$LASTEXITCODE` check, on the Launcher build at line 106, and none around its `Kape22ProductionDataParityTests` invocation at line 188 (item-4).

**Why this is a retro-level finding, not a re-statement of what deferred-work.md already says:** `deferred-work.md`'s own entries for these four items (dated 2026-09-17) correctly describe them as "candidate for Story 4.9's hardening pass" — a *proposal*, hedged with "candidate." The `sprint-status.yaml` `ref` field turned that proposal into what reads like a routing commitment, and nothing in the subsequent Story 4.9 spec, code review, or `/run-review` pass (`reviews/story-4-9/aggregated-report.md`) revisited whether Story 4.9's actual four-item AC still covered them — it didn't, and the mismatch between "ref says 4.9" and "4.9's frozen scope says A-2..A-5 only" was never reconciled by any of the three stories in between.

**Disposition:** fix now (routing correction) — these four items are not resolved and have no story currently claiming them; each is real, sourced, and independently confirmed unaddressed. Routed as action items B-1 through B-4 below, with corrected `ref`s.

### 2.5 A fifth Story 4.3-bis review finding — `DecimalScale.Apply` validates scale but not magnitude — was never promoted to a tracked action item at all

**Source:** `deferred-work.md` §"Deferred from: code review of story-4.3-bis (2026-09-18)" (the second, later-dated 4.3-bis review section, distinct from the four items in §2.4 above): `DecimalScale.Apply` (`src/Kape22Importer/DecimalScale.cs:12`) divides by a power of ten to fix the decimal point but never checks the scaled result against its target column's total `DECIMAL(p,s)` precision — an outlier raw KAPE22 int whose *scaled* result still exceeds a column's magnitude limit would still throw a raw, undiagnosed SQL overflow, the same failure class Story 4.3-bis fixes, narrowed but not closed. The entry itself already names this "candidate for Story 4.9's hardening pass," but — same as §2.4 — Story 4.9's frozen AC never included it, and `sprint-status.yaml`'s `action_items` list has no entry for it at all (verified: no `grep -i magnitude` / `precision.*guard` hit in the file). It exists only as prose in `deferred-work.md`, unlike the §2.4 items which at least have stale-but-present `action_items` entries.

**Disposition:** fix now (tracking gap) — this is a real, sourced, correctly-deferred-at-the-time finding that fell out of the tracking system entirely between the review that raised it and this retro. Routed as action item B-5 below.

### 2.6 Story 4.9's own review deferrals (F-1..F-4) remain correctly disposed — not re-opened here

**Source:** `reviews/story-4-9/aggregated-report.md` §4 and `deferred-work.md` §"Deferred from: code review of story-4.9 (2026-09-18)": (F-1) the A-5 pre-check reports only the first colliding `CodeOperation` group, not every group; (F-2) the missing-Coulee and Consignes-collision checks in `PersistMapped` are sequential early-returns, not accumulated; (F-3) no test exercises a blank/whitespace-only `OF`/`Coulee` through the new unconditional trim; (F-4) the A-5 REJETÉ message names the colliding `CodeOperation` but not which two sections collided.

**Disposition:** accept as-is, unchanged from the per-story review's own disposition. Each was explicitly checked against Story 4.9's frozen spec boundary and found genuinely out of scope (F-1/F-2: the spec's own AC text describes "a collision," singular, and the sequential-check pattern is `Kape22Persister`'s pre-existing convention) or a coverage-only gap with no correctness risk (F-3: `Kape22FileMessage.OF`/`.Coulee` default to `string.Empty`, never `null`, verified by inspection). Re-litigating these would be exactly the busy-work `references/acceptance-verdict.md` warns against — a finding already routed with evidence does not need a second disposition from a different reviewer reaching the same place. Not routed as new action items.

### 2.7 Architecture delta since the first retro: moderate, expected growth, no new god-class

**Source:** `git_evidence.py` churn for this range vs. current file sizes (`wc -l`, this session): `Kape22Persister.cs` grew from 244 lines (first retro) to **277 lines** (+33, the A-5 pre-check block) — proportionate to the one new control it gained, not runaway growth. `DecimalScale.cs` is new, 22 lines, single-purpose (AD-2-compliant: `internal static`, zero reflection, zero DB access, explicitly modeled on the pre-existing `SectionChargeApplicability` precedent per its own header comment). No file in the range crosses into god-class territory; the largest test file touched, `TransactionalPersistenceTests.cs` (590 lines now, +299 net this range across 4 commits), is a single-responsibility integration-test file whose growth tracks the number of atomicity scenarios it proves (AD-1), not accreted unrelated concerns.

**Disposition:** accept as-is. No action item.

---

## 3. Behavior verification

- `dotnet build TextToXml.sln -warnaserror` → **0 warnings, 0 errors** (this session, 2026-09-18).
- `dotnet test TextToXml.sln --filter Category=Unit` → **673 passed, 0 failed, 0 skipped** (192 `TextToXml.Tests` + 481 `Kape22Importer.Tests`, this session) — up from 662 at the first retro (+11: `DecimalScaleTests.cs` and the extended `OrdreFabricationMapperTests.cs`/`SectionCharge*MapperTests.cs`/`Kape22MapperTests.cs`/`Kape22FichierProcessorTests.cs` cases added by 4.3-bis and 4.9).
- `dotnet test TextToXml.sln --filter Category=Integration` → **140 passed, 0 failed, 0 skipped**, against the local SQL Server harness (AR-12) — up from 136/1-skipped at the first retro. The single prior skip (`GpaoImportP60WorkerEndToEndTests`) now runs and passes against the real, byte-for-byte `P60_847_682_081/082` fixtures — this is the epic's strongest behavior proof: the worker's full pipeline (Launcher → `Client.RunTickCore` → `Kape22FichierProcessor` → `Kape22ImportBundleMapper` → `Kape22Persister`, real SQL Server, real files) now runs end to end and its output is cross-checked against `Kape22ProductionDataParityTests` for production-mirroring parity, where the first retro's run could only report this test as skipped.
- No fresh manual exercise of the pipeline was performed beyond the existing automated suites; as with the first retro, `EndToEndImportIntegrationTests`, `RejectionAtomicityIntegrationTests`, and now `GpaoImportP60WorkerEndToEndTests` unskipped constitute the epic's own E2E proof.

---

## 4. Previous-retro follow-through

**Scope note:** the standard N-1 rule points at `epic-3-retro-2026-09-11.md`; its items were already fully reconciled by the first Epic 4 retro (its §4) and nothing in stories 4.2-bis/4.3-bis/4.9 touches Epic 3 scope, so there is nothing new to check there. The directly relevant predecessor for this retro is `epic-4-retro-2026-09-17.md` itself — the same epic's own prior retrospective, an arrangement the skill's usual "earlier epic" framing does not anticipate but whose intent (check whether the previous retro's commitments landed) applies exactly the same way here.

| Item (`id`) | `sprint-status.yaml` status | Evidence checked this session | Argued status |
|---|---|---|---|
| `story-4-6-decimal-scale-defect-story-4-3-bis` | done | §2.3 — `DecimalScale.cs`, 21 call sites, `ZeroOutOfScaleDimensions` removed, `GpaoImportP60WorkerEndToEndTests` now green | Confirmed `done`, holds |
| `epic-4-retro-item-1-annex-scale-precision-field` (A-1) | **open** | §2.1 — annex `Scale` column, `MappingAnnexSchema.cs`, `MappingAnnexCompletenessTests.cs` all confirm the AC is fully met | **Argue `done`** — stale entry, propose reconciliation |
| `epic-4-retro-item-2-unit-tests-assert-downstream-tables` (A-2) | done | §2.2 — `Kape22FichierProcessorTests.cs:218-230` | Confirmed `done`, holds |
| `epic-4-retro-item-3-trim-of-coulee-at-source` (A-3) | done | §2.2 — `Kape22Mapper.cs:112-113` | Confirmed `done`, holds |
| `epic-4-retro-item-4-shared-cold-coulee-constant` (A-4) | done | §2.2 — `Kape22ImportBundle.cs:19`, both consumers reference it | Confirmed `done`, holds |
| `epic-4-retro-item-5-widen-persister-exception-filter` (A-5) | done | §2.2 — pre-check added, filter deliberately not widened per frozen AC | Confirmed `done`, holds |
| `story-4-3-bis-review-item-1-scale-literal-triplication` | open, ref → story-4-9 | §2.4 — no consistency test exists; Story 4.9's frozen AC never covered it | Still `open` — **`ref` needs correcting**, not the status |
| `story-4-3-bis-review-item-2-no-anti-bypass-guard` | open, ref → story-4-9 | §2.4 — no reflection/analyzer guard exists | Still `open` — **`ref` needs correcting** |
| `story-4-3-bis-review-item-3-production-parity-scope-gap` | open, ref → story-4-9 | §2.4 — `Kape22ProductionDataParityTests.cs:141` still `L_D_KAPE22`-only | Still `open` — **`ref` needs correcting** |
| `story-4-3-bis-review-item-4-e2e-script-exit-code-gap` | open, ref → story-4-9 | §2.4 — no `$LASTEXITCODE` check around the parity-test block | Still `open` — **`ref` needs correcting** |

**Not in `sprint-status.yaml` at all:** the `DecimalScale.Apply` magnitude-guard finding (§2.5) — recorded here as a tracking gap, routed as a brand-new action item (B-5) rather than a status correction, since there is nothing to correct.

Six of the eleven items this retro checked are confirmed landed (the defect fix plus A-2 through A-5). One (A-1) landed but was never marked so. Four (the Story 4.3-bis review items) were never landed at all, despite `sprint-status.yaml` implying a routing to Story 4.9 that Story 4.9's own frozen scope never actually included.

---

## 5. Action items (this retro)

Numbered `B-` to stay distinct from the first retro's own `A-1`..`A-5` (§2.2, all landed) and from `story-4-6-decimal-scale-defect-story-4-3-bis` (§2.3, landed).

**Sprint-status note:** B-1 through B-4 are this retro's correction of an existing routing error (§2.4) — they are the same work already tracked as `story-4-3-bis-review-item-1` through `-item-4` in `sprint-status.yaml`, just with the record set straight on where that work actually belongs (not Story 4.9). The `sprint_status.py` tooling has no flag to rewrite an item's `ref` in place, and hand-editing the file is a hard rule violation, so Phase 5 does **not** add B-1..B-4 as new, duplicate `sprint-status.yaml` entries — the pre-existing four entries stand, and this document is the durable correction for their stale `ref`. Only **B-5** is genuinely new to tracking (§2.5) and gets a fresh `sprint-status.yaml` entry.

| ID | Action | Detail | Owner | Status | Ref |
|---|---|---|---|---|---|
| **B-1** | Build the mapper-source-vs-annex scale consistency check | §2.4 (was `story-4-3-bis-review-item-1`, mis-routed): nothing cross-checks the 21 hand-written `DecimalScale.Apply(source.X, N)` literal scale arguments across the 5 mappers against the annex's own `Scale` column; a wrong digit copy-pasted consistently into a mapper and its test would pass silently. `MappingAnnexCompletenessTests`'s existing `ScaleCompletenessAgainstModel` family checks the annex has a `Scale` for every qualifying EF column — it does not check that shipped mapper code uses that same value. | Dev | open | `deferred-work.md` §"Deferred from: code review of story-4.3-bis (2026-09-17)" item 1; this doc §2.4 |
| **B-2** | Guard against a future mapper bypassing `DecimalScale.Apply` | §2.4 (was `story-4-3-bis-review-item-2`, mis-routed): no analyzer or reflection-based completeness test prevents a future 6th mapper from assigning a raw KAPE22 `int`/`int?` directly to a narrow `decimal` EF column, reproducing the exact defect Story 4.3-bis fixed. | Dev | open | `deferred-work.md` §"Deferred from: code review of story-4.3-bis (2026-09-17)" item 2; this doc §2.4 |
| **B-3** | Extend `Kape22ProductionDataParityTests` to the downstream decimal-scale tables | §2.4 (was `story-4-3-bis-review-item-3`, mis-routed): `MappedFichier_MatchesLegacyProductionRow` only round-trips `L_D_KAPE22`; no test in the verification chain compares a persisted, `DecimalScale`-scaled downstream column (`L_D_ORDRE_FABRICATION`, the `L_D_SECTIONCHARGE_*` tables) against a known-correct real production value. | Dev | open | `deferred-work.md` §"Deferred from: code review of story-4.3-bis (2026-09-17)" item 3; this doc §2.4 |
| **B-4** | Check `$LASTEXITCODE` after the `Kape22ProductionDataParityTests` step in `e2e-worker-import.ps1` | §2.4 (was `story-4-3-bis-review-item-4`, mis-routed): the script's closing `dotnet test ... Kape22ProductionDataParityTests` invocation is unguarded, unlike the earlier Launcher build step; a failing production-parity comparison would not fail the wrapping process. Cheap, mechanical — same shape as the guard already present at line 106. | Dev | open | `deferred-work.md` §"Deferred from: code review of story-4.3-bis (2026-09-17)" item 4; this doc §2.4 |
| **B-5** | Add a magnitude/precision guard to `DecimalScale.Apply`, or an equivalent pre-persist check | §2.5: `DecimalScale.Apply` corrects decimal placement (scale) but never validates the scaled result against each column's total `DECIMAL(p,s)` magnitude; an outlier raw KAPE22 int whose scaled result still exceeds a column's precision limit still throws a raw, undiagnosed SQL overflow — the same failure class Story 4.3-bis fixed, narrowed but not closed. Not currently tracked as a sprint-status action item at all (§2.5). | Dev / Lead Architecte (may need a design choice on where the guard lives) | open | `deferred-work.md` §"Deferred from: code review of story-4.3-bis (2026-09-18)"; this doc §2.5 |

No fix-now findings from this pass require **spec reconciliation** — the mismatches found (§2.4) are tracking/routing errors in `sprint-status.yaml`, not divergences between the as-built code and `epics.md`'s frozen AC text.

**Process lesson:** when a deferred-work entry names a *future* story as its likely home ("candidate for Story 4.9's hardening pass"), that naming is a proposal, not a commitment — nothing currently re-verifies, at the point the named story's own spec is frozen, that its actual AC still covers what was promised. Two mechanisms would have caught this before it reached a retro: (a) the story spec's own "Ask First" scoping step cross-referencing every `ref: -> story-N` pointer in `sprint-status.yaml` naming that story, flagging any the frozen AC does not cover; or (b) this retro's own Previous-retro follow-through check running one epic earlier — but the first retro could not have caught this, since Story 4.9 did not exist yet when it ran. (a) is the actionable one; not routed as its own action item here since it would change `bmad-build`'s spec-freeze step rather than this project's code, and is offered as an open question instead (§7, Q-4).

---

## 5bis. Start / Stop / Continue

*(Added per `/retro-epic`'s own report-completeness check; not part of `bmad-retrospective`'s standard document template, which routes the same substance through §2 Findings / §5 Action items / §7 Open questions instead. Each line below cites the section it is drawn from rather than asserting anything new.)*

**Start:**
- Cross-checking a `sprint-status.yaml` action item's `ref` against the *actual* frozen AC of the story it points to, at the point that story's spec is frozen — not only at the next retro (§5 process lesson, Q-4). This retro's single biggest finding (§2.4) was four items silently falling through exactly this gap.
- Treating a "candidate for story N" note in `deferred-work.md` as a proposal requiring confirmation, not a routing commitment — and confirming it when story N's own spec is written (§2.4, §2.5).

**Stop:**
- Letting a deferred-work entry's suggested destination story stand in for an actual `sprint-status.yaml` action item. §2.5's magnitude-guard finding was correctly deferred at review time but never got an `action_items` entry at all — it only existed as prose, and was invisible to every mechanism that tracks open work until this retro read the file directly.

**Continue:**
- Reopening an epic via a scoped correct-course (`sprint-change-proposal-2026-09-17.md`) with a strict story sequence (4.2-bis → 4.3-bis → 4.9) rather than folding fixes into ad-hoc patches — this resolved Q-1 exactly as the first retro hoped, and left a clean, individually-reviewable commit per concern (§7 Q-1, §1).
- Per-story `/run-review` passes catching real, non-blocking findings and disposing them with evidence and justification inline (§2.6) — Story 4.9's own review correctly kept F-1..F-4 out of the fix-now path without this retro needing to re-derive that judgment.
- Verifying a "done" claim against source before trusting it, rather than trusting `sprint-status.yaml` at face value — every landed item in §2.1/§2.2/§2.3 was confirmed by reading the actual file/line, not by reading the status column alone; this is also what caught §2.1's stale entry and §2.4's mis-routed ones.

---

## 6. Acceptance verdict

### **ACCEPTED-WITH-OPEN-ITEMS** (declared criteria)

**Declared criteria:** `epics.md` §Epic 4, stories 4.1 through 4.9 (`AC-FR17-1`..`AC-FR21-5`, plus 4.2-bis/4.3-bis/4.9's own AC blocks), `ARCHITECTURE-SPINE.md` (AD-1..AD-7), CC-1..CC-5/CC-7 per story, all declared not profiled.

**Evidence supporting acceptance:**
- 10/10 stories `done`, `pending_stories` empty → machine verdict is not forced to `rejected`.
- Build clean (0 warnings/errors under `-warnaserror`), 673/673 Unit tests green, **140/140 Integration tests green with zero skips** — an improvement over the first retro's one known, documented skip, and direct proof the epic's one production-blocking defect is closed.
- Every action item the first retro opened (A-1 through A-5) is now landed in the code, four of five already correctly marked `done` in `sprint-status.yaml` (§4).
- AD-1, AD-2, AD-3, AD-4, AD-7 continue to hold as designed (§2.7) — no new architecture/layering violation, no god-class growth, moderate proportionate file growth only.
- The Story 4.9 AC's own explicit design constraint (pre-check, not a widened exception filter) was verifiably honored, not quietly abandoned for convenience (§2.2).

**Open items (non-blocking for closing the epic, tracked):**
- **Bookkeeping only**: A-1 (§2.1) — landed but unrecorded; propose reconciling in Phase 5.
- **Genuinely still open, newly re-routed this retro**: B-1 (scale-literal consistency check), B-2 (anti-bypass guard), B-3 (production-parity scope gap), B-4 (e2e script exit-code gap), B-5 (magnitude guard, previously untracked).

None of B-1 through B-5 contradicts a declared AC or was caught failing a test — each was explicitly scoped out of the story that raised it (Story 4.3-bis's own "Ask First: None" boundary, or Story 4.9's frozen four-item AC) and is process/robustness hardening for a defect class already fixed at its currently-known instances, not a defect in what shipped. Hence **accepted-with-open-items**, consistent with the first retro's own bar for the same epic.

---

## 7. Open questions

- **Q-1** (first retro, resolved this session): should Story 4.3-bis land before the annex extension (A-1), or alongside it? **Resolved**: the correct-course explicitly ordered 4.2-bis → 4.3-bis → 4.9 (`sprint-change-proposal-2026-09-17.md`), and the commit history confirms 4.2-bis's commits (`3b92a2b`, `13a6105`) precede 4.3-bis's (`a277925` onward) in the range. A-1 landed first as recommended.
- **Q-2** (first retro, carried forward, no new evidence this session): can real KAPE22 data have a nonzero `NombreDemiProduit` on an OF whose `CodeOpeRefroidissoir`/`RangOpeRefroidissoir` are empty? If so, `AC-FR20-2`'s ingot/furnace distribution control silently never runs for that Fichier. Needs a legacy-data read, not a code decision; not addressed by any of 4.2-bis/4.3-bis/4.9.
- **Q-3** (first retro, carried forward, no new evidence this session): can the same OF legitimately be resubmitted under a different `NumeroFichier`? The D22 anti-duplicate guard does not cover `OrdreFabrication`/`SectionCharge*`/`Consignes` the way it covers `Coulee`. Still unaddressed.
- **Q-4** (new this retro, §5 process lesson): should `bmad-build`'s spec-freeze step for a story cross-check every `sprint-status.yaml` action item whose `ref` names that story, and flag any the frozen AC does not actually cover? This would have caught §2.4's four mis-routed items before Story 4.9 closed rather than at this retro. A process/tooling decision, not a code decision for this project.

---

## 8. Scope decisions (interactive run)

- **Epic selected**: 4, supplied explicitly (`/retro-epic 4`). `detect-epic --epic 4` → `story_count: 10`, `pending_stories: []`, `retro_status: done` (already set by the first retro).
- **Diff range**: `ce6a7fc75af7b89695c7c2eaf6d5654cce44c323^..HEAD` — parent of story 4.1's first commit to `HEAD`; recomputed fresh this session (28 commits vs. the first retro's 16, reflecting the correction-arc commits added since).
- **`--stories` matching note**: `git_evidence.py`'s story attribution matches literal substrings against commit subjects; subjects use dotted story ids (`story-4.9`), so `--stories` was passed as `4.1,4.2,...,4.2-bis,4.3-bis,4.9` (dots, not the sprint-status.yaml key's hyphens) to get correct per-commit attribution.
- **Review scope, deliberately narrowed** (see Evidence inventory note above): no fresh multi-agent `bmad-review` pass over the full diff; this retro instead verified, against direct source reads, whether the first retro's and the per-story reviews' own deferred items actually landed — the check no diff-scoped review can perform on its own.
- **Interactive greeting step**: skipped without blocking, per this session's standing instruction to proceed on reasonable calls under Auto Mode rather than pause for a check that had no decision riding on it; no going-in concern was solicited. Recorded here rather than treated as a headless assumption, since this run is otherwise fully interactive and all evidentiary decisions above are auditable from cited sources, not machine defaults.
- **Session logs**: unavailable, as in every prior epic retro.
