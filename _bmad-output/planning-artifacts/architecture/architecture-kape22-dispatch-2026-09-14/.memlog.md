---
scope: Dispatch de L_D_KAPE22 vers L_D_ORDRE_FABRICATION, L_D_COULEE, L_D_CONSIGNES et les tables L_D_SECTIONCHARGE_* (post Story 2.8), en remplacement de la reflexion legacy (KAPE22Controller._ConvertKAPE22toOrdreFabrication + MappingTemplate)
purpose: build-substrate
altitude: feature
updated: 2026-09-14T14:23
---

- (constraint) User directive: dispatch rewrite must be reflection-free — each target table gets its own explicit mapper/persister, no generic PropertyInfo walk.
- (constraint) User directive: all target tables (L_D_ORDRE_FABRICATION, L_D_COULEE, L_D_CONSIGNES, all touched L_D_SECTIONCHARGE_*) are written in one all-or-nothing transaction; any single failure rolls back everything.
- (constraint) User directive: on failure, the precise cause must be identified and logged so the operator is warned — reuse/extend the existing double journal (MQTTnetServices.Logs + L_D_LOG_COMMANDE, FR-14/Story 3.3).
- (assumption) Legacy code inspected: Desktop/kape22/InterfaceManager.ImportGPAO -> KAPE22Controller.SetOrdreFabrication -> _ConvertKAPE22toOrdreFabrication (KAPE22Controller.cs) drives an XML-described MappingTemplate (Objet/Propriete, RefObject for nested entities) walked via System.Reflection.PropertyInfo to populate an EF ObjectContext entity graph (AddRef for navigation properties: OrdreFabrication + nested ConsignesXxx/Couleee), then OrdreFabricationController.AddRange2 persists. This is the procedure being replaced.
- (assumption) Legacy L_D_SECTIONCHARGE_* family observed in OrdreFabricationController.cs (DeleteOF/AnnulationOF symmetric cleanup): CHUTAGE, LINGOT, DECOUPE, PITS, POIDSMETRIQUE, REFROIDISSOIRS, SVT -- list to confirm exhaustive with user/DB schema before freezing AD.
- (assumption) Current repo (TextToXml.sln, Kape22Importer) already ships Epics 1-3: TextToXml conversion, Kape22Mapper/Kape22Persister inserting L_D_KAPE22 transactionally (FR-11/Story 2.8), worker orchestration with double journal (FR-14/Story 3.3). This new work is the next stage consuming the persisted L_D_KAPE22 row (or the in-flight entity before commit) to dispatch into downstream tables.
- (decision) AD candidate: single extended transaction. L_D_KAPE22 insert (Kape22Persister, Story 2.8) and all downstream dispatch tables (OrdreFabrication, Coulee, Consignes, SectionCharge_*) commit or rollback together, to preserve NFR-7 replay semantics against the existing anti-doublon guard (AC-FR11-6/7).
- (decision) AD [ADOPTED]: L_D_SECTIONCHARGE_* target list frozen at 7 tables: CHUTAGE, LINGOT, DECOUPE, PITS, POIDSMETRIQUE, REFROIDISSOIRS, SVT.
- (decision) AD [ADOPTED]: legacy pre-persistence business validations (cold Coulee must already exist in L_D_COULEE, hot Coulee number must start with 0, ingot/four distribution must match NombreDemiProduit) are in scope. A violation is a blocking rejection (same shape as an AC-FR11-4 rejected Fichier): logged with its precise cause, no rows written, Fichier moves to error/.
- (decision) AD [ADOPTED]: a dedicated story extracts the legacy MappingTemplate XML + entity classes into an explicit, documented field-by-field mapping annex (target table, column, source L_D_KAPE22 field/derivation rule) before the mapper/persister code is written for that table.
- (decision) AD [ADOPTED], user-stated, load-bearing: strict scope wall. This work is a pure continuation of the Kape22Importer/TextToXml codebase already built (Epics 1-3), executed by the existing worker (Client:Publisher, MicroServices.sln, Story 3.4) which currently runs on the MQTT server. The legacy Ascometal.LSI.DAL/BLL application is read-only reference material for understanding intent; it is NEVER modified and NEVER called at runtime by the new code (no assembly reference, no shared DB context type). Once this ships, the legacy ImportGPAO path is unplugged and this worker becomes the sole writer of these tables for the GPAO/P60 flow.
- (decision) AD [ADOPTED]: composition = Option A, extended bundle. A new orchestrating mapper calls Kape22Mapper.Map then the explicit per-table mappers (pure, no DB access) to produce a Kape22ImportBundle {L_D_KAPE22, L_D_ORDRE_FABRICATION, L_D_COULEE, L_D_CONSIGNES[], SectionCharge rows (7, each nullable/optional)}. Kape22Persister is extended to accept the bundle: it runs the DB-reading business validations (Coulee existence etc, needs the live context) right before staging, stages every non-null entity with context.Add, and keeps its single SaveChanges/catch/PersistenceError shape unchanged in kind, just covering more entities.
- (decision) AD [ADOPTED], ratifies AR-8/AR-12: new EF entities (L_D_ORDRE_FABRICATION, L_D_COULEE, L_D_CONSIGNES, 7x L_D_SECTIONCHARGE_*) are added database-first to AscoLsiDbContext, one file per entity under src/Kape22Importer/Persistence/, no migrations, matching the existing L_D_KAPE22.cs style; scripts/schema/ gains the matching idempotent CREATE TABLE scripts generated from AFV004-LSI (R-3 convention) so AR-12 integration tests cover them.
- (decision) AD [ADOPTED], ratifies FR-6/CC-5: new failure causes (Coulee introuvable, repartition lingots incoherente, per-table mapping/consigne lookup failures) are new ErrorCode members surfaced as ConversionError{Block:File}, flowing through the existing rejected-Fichier path (L_D_LOG_COMMANDE REJETE, error/ folder, no partial writes) -- no new error contract invented.
- (decision) AD-6 [self-review finding, closed]: Kape22ImportBundle carries the same metadata as MapResult<L_D_KAPE22> (Success/Errors/Warnings/NumeroFichier/OF). Kape22Persister.Persist is replaced (not overloaded) to take the bundle; Kape22FichierProcessor updated in the same story. No parallel Persist surface.
- (decision) AD-7 [self-review finding, closed]: inter-entity linkage in the bundle is explicit scalar FK columns (OF, owning SectionCharge key) set by mappers, never EF navigation-property graphs (no AddRef-style wiring).
- (question) Open, deferred to the mapping-extraction story: which of the 7 L_D_SECTIONCHARGE_* tables apply to a given OF (legacy does not appear to always populate all 7) -- needs the MappingTemplate/legacy business-rule read to answer, not guessed.
- (event) spine finalized
